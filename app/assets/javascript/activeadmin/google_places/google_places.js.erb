var imported = document.createElement('script');
imported.src = 'https://maps.googleapis.com/maps/api/js?key=<%= ENV["GOOGLE_PLACES_API_KEY"] %>&libraries=places';
document.head.appendChild(imported);